# dotfiles

```shell
cd && git clone --recurse-submodules https://github.com/sedlund/dotfiles
cp -a dotfiles/. . && rm -rf dotfiles
git reset --hard
git config --local status.showuntrackedfiles no
```

