#!/bin/sh

find . -regextype egrep -regex ".*\.(avi|mp4|mkv|webm)$" -print0 \
  | xargs -0 -I{} -P"$(nproc)" \
    sh -c " \
      ffprobe \"{}\" 2>&1 \
        | grep -E 'Input|bitrate' \
        | sed -z 's#Input.*from ##; s# start: .*,##; s#:\n #:#' \
      " \
  | sort -t: -n -k6
